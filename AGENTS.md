# X Muted Words

面向 X 中文用户的原生屏蔽词词包、批量添加扩展和可选本地评论过滤。

## 规则

- 屏蔽词只维护在 `packs/manifest.json` 和 `packs/*.json`。
- 原生屏蔽词流程只添加，不删除用户已有词。
- 不读取 Cookie，不调用 X 私有 API，不上传页面内容。
- 本地评论过滤默认关闭，只影响当前浏览器。
- 修改扩展后至少运行 README“开发检查”中的命令。
- 未经明确要求，不提交 `dist/`、ZIP、日志或浏览器运行数据。
